# Inherit staticcheck's default checks, minus U1000 (unused code): the orphaned
# standard-confidential helpers (confidential_aws.go / confidential_azure.go, left
# dead by the measured-profile refactor) still trip it. Remove "-U1000" once they
# are deleted so dead code is caught too.
checks = ["inherit", "-U1000"]
